<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorWhiteHeader</item>
        <item name="android:buttonStyle">@style/Widget.AppCompat.Button</item>
        <item name="android:buttonCornerRadius">50dp</item>
    </style>


    <style name="AppTheme.ActionBar.Logo" parent="AppTheme">
        <item name="android:logo">@drawable/dreamboat_logo</item>
        <item name="android:displayOptions">useLogo|showHome</item>
        <item name="colorPrimary">@color/colorWhiteHeader</item>
        <item name="actionBarSize">40dp</item>
        <item name="actionBarStyle">@dimen/fab_margin</item>
    </style>
    <style name="hintStyle">
        <item name="android:layout_centerVertical" />
    </style>

    <style name="PrimaryColoredButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:radius">50dp</item>
        <item name="android:buttonCornerRadius">50dp</item>
        <item name="colorButtonNormal">@color/colorPink</item>
        <item name="colorControlHighlight">@color/colorWhiteHeader</item>
        <item name="android:textColor">@color/colorWhiteHeader</item>

    </style>

    <style name="AppTheme.ActionBar.Transparent" parent="AppTheme">
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="colorPrimary">@android:color/transparent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--<style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />-->

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

</resources>
